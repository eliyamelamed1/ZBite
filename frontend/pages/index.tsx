import React from 'react';
import { useSelector } from 'react-redux';

const HomePage = () => {
    return (
        <React.Fragment>
            <div>home page</div>
        </React.Fragment>
    );
};

export default HomePage;
