setup:
    addons:
        - plan: heroku-postgresql
        - plan: bonsai-elasticsearch
build:
    docker:
        web: /api/Dockerfile
release:
    image: web
    command:
        - python manage.py collectstatic --noinput

run:
    web: gunicorn config.wsgi
